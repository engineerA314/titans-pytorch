# titans-pytorch requirements (auto-generated by pipreqs, manually adjusted)

# Core
torch>=2.4
einops>=0.8
einx>=0.3
numpy>=2.0
tqdm>=4.60

# Positional embeddings
rotary_embedding_torch>=0.8
axial_positional_embedding>=0.3

# Attention & Transformers
x_transformers>=2.0
hyper_connections>=0.2

# Associative scan (GPU parallelization)
assoc_scan>=0.0.2
accelerated_scan>=0.2

# Tensor utilities
tensordict>=0.10

# Optional (for training examples)
# wandb>=0.23
# pytest>=8.0

# Note: triton is required for GPU acceleration on Linux
# triton>=3.0  # Linux only
